name: Publish
on:
  release:
    types:
      - released
  workflow_dispatch:

jobs:
  publish:
    uses: srnyx/tools/.github/workflows/publish.yml@main
    with:
      modrinth: GVSyHi8e
      curseforge: 807393
    secrets: inherit
    permissions:
      contents: write
